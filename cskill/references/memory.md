# Memory Checklist

- 先定义所有权：谁申请，谁释放。
- 申请后立刻判空。
- 先查长度和乘法溢出，再分配。
- `realloc` 先接到临时指针。
- 初始化后再读。
- 不要越界读写。
- 字符串给 `'\0'` 留空间。
- 不要返回栈内存地址。
- `free` 后不要再用。
- 同一块内存不要重复释放。
- 错误路径和正常路径都要完整清理。
- 外部输入的长度先校验。
- 调试时开 `ASan`、`UBSan`、`valgrind`。
